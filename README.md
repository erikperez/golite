#golite
A Super Light HTTP Server that will remind you of `python -m SimpleHTTPServer` in the usage and purpose.


##Getting started
- Do `go install golite`

### Usage:
`golite -port <port>(default:8080) -root <directory to serve>(default:./root)`

